<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vbuecker.app_lanamento_dados_devventure">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App_Lançamento_Dados_DevVenture">
        <activity android:name="com.vbuecker.app_dice_play_devventure.SpecialActivity"></activity>
        <activity android:name="com.vbuecker.app_dice_play_devventure.UserActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.vbuecker.app_dice_play_devventure.MainActivity"></activity>
    </application>

</manifest>